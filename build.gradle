allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    group = 'com.romanwit'
    version = '0.0.1-SNAPSHOT'
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

